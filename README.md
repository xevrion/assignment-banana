# assignment-banana

This is an assignment generator which generates handwritten images of assignments by just the PDF by using google's nano-banana model.

ISSUE: ran into a barrier where you have to pay to use the nano-banana model. the free API key only allows text based generation or image to text understanding but not text to image generation.
